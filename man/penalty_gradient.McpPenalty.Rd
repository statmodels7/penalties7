% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scad_mcp.R
\name{penalty_gradient.McpPenalty}
\alias{penalty_gradient.McpPenalty}
\alias{penalty_hessian.McpPenalty}
\title{Coefficient Derivatives of an MCP Penalty}
\arguments{
\item{pen}{An \code{\link[=McpPenalty]{McpPenalty()}} object.}

\item{beta}{A numeric vector of length \code{pen@n_coef}.}

\item{theta}{A list holding \code{lambda} and \code{gamma}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
\code{penalty_gradient()} a numeric vector of length \code{pen@n_coef};
\code{penalty_hessian()} a symmetric \code{pen@n_coef} by \code{pen@n_coef} base matrix,
diagonal under the identity map and negative semidefinite always.
}
\description{
\code{penalty_gradient()} returns MCP's defining derivative carried back through
the map, and \code{penalty_hessian()} its second derivative, which is
\eqn{-1/\gamma} inside the taper and zero beyond it.
}
\details{
On each coordinate, with \eqn{u = \lvert t\rvert} and \eqn{s} its sign,

\deqn{\frac{\partial\rho}{\partial t}
    = s\,(\lambda - u/\gamma)_{+}, \qquad
  \frac{\partial^2\rho}{\partial t^2} =
  \begin{cases} -1/\gamma & u \le \gamma\lambda \\
    0 & u > \gamma\lambda. \end{cases}}

The Hessian is \strong{negative} wherever the penalty is doing anything, which is
the non-convexity MCP is named for, and it is constant there: MCP is the
penalty with the least curvature among those that threshold at rate
\eqn{\lambda} and reach zero bias by \eqn{\gamma\lambda}. A penalized
objective stays convex only while the likelihood's own curvature exceeds
\eqn{1/\gamma}.

At \eqn{t = 0} the sign is zero, so the gradient returned is \code{0}: one
element of the subdifferential \eqn{[-\lambda, \lambda]}. Selection happens
through \code{\link[=penalty_prox]{penalty_prox()}}.
}
\examples{
pen <- mcp_penalty(n_coef = 3)
th <- list(lambda = 1, gamma = 3)

# The gradient falls linearly and is exactly zero past gamma * lambda.
penalty_gradient(pen, c(0.5, 2, 5), th)

# The curvature is a constant -1/gamma inside the taper.
diag(penalty_hessian(pen, c(0.5, 2, 5), th))

}
\seealso{
\code{\link[=penalty_value.McpPenalty]{penalty_value.McpPenalty()}} for the quantity differentiated,
\code{\link[=penalty_grad_theta.McpPenalty]{penalty_grad_theta.McpPenalty()}} for the hyperparameter blocks,
\code{\link[=penalty_gradient.ScadPenalty]{penalty_gradient.ScadPenalty()}} for the sibling family.
}
\keyword{internal}
