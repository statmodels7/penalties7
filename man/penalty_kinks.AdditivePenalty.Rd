% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/additive_penalty.R
\name{penalty_kinks.AdditivePenalty}
\alias{penalty_kinks.AdditivePenalty}
\alias{is_proper.AdditivePenalty}
\title{Smoothness and Properness of an Additive Penalty}
\arguments{
\item{pen}{An \code{\link[=AdditivePenalty]{AdditivePenalty()}} object.}

\item{theta}{A named list of \code{lambda1}, \code{lambda2}, ... Read by
\code{penalty_kinks()}, whose answer does not depend on it.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
\code{penalty_kinks()} a numeric vector of length zero.
\code{is_proper()} a single logical.
}
\description{
\code{penalty_kinks()} returns \code{numeric(0)}, the value being a quadratic form and
smooth everywhere. \code{is_proper()} returns \code{TRUE} when the stored rank equals
the number of coefficients.
}
\details{
The branch registers no \code{is_quadratic()} method, so it inherits \code{FALSE} from
\code{\link[=penalty]{penalty()}} even though the value is a quadratic form. See
\code{\link[=additive_penalty]{additive_penalty()}} for what that costs.

Properness is the rank of the sum against the number of coefficients, and
the rank is fixed at construction. The two components of an anisotropic
tensor smooth intersect in a null space of their own, so such a penalty is
improper: a 4 by 4 grid penalized by curvature along each margin has rank 12
out of 16.
}
\examples{
P <- crossprod(diff(diag(4), differences = 2))
te <- additive_penalty(list(kronecker(diag(4), P), kronecker(P, diag(4))))

penalty_kinks(te, list(lambda1 = 1, lambda2 = 1))
is_proper(te)
c(rank = penalty_rank(te), n_coef = te@n_coef)

# A sum of full-rank components is proper.
is_proper(additive_penalty(list(diag(3), diag(c(2, 1, 1)))))

}
\seealso{
\code{\link[=penalty_kinks]{penalty_kinks()}} and \code{\link[=is_proper]{is_proper()}} for the generics,
\code{\link[=additive_penalty]{additive_penalty()}} for why \code{\link[=is_quadratic]{is_quadratic()}} answers \code{FALSE} here.
}
\keyword{internal}
