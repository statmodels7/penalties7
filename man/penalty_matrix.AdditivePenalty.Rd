% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/additive_penalty.R
\name{penalty_matrix.AdditivePenalty}
\alias{penalty_matrix.AdditivePenalty}
\alias{penalty_rank.AdditivePenalty}
\alias{penalty_logpdet.AdditivePenalty}
\alias{penalty_null_basis.AdditivePenalty}
\title{Marginal Quantities of an Additive Penalty}
\arguments{
\item{pen}{An \code{\link[=AdditivePenalty]{AdditivePenalty()}} object.}

\item{theta}{A named list of \code{lambda1}, \code{lambda2}, ... \code{penalty_matrix()}
and \code{penalty_logpdet()} only.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
\code{penalty_matrix()} a symmetric base matrix of side \code{pen@n_coef}.
\code{penalty_rank()} a single integer, and \code{penalty_null_basis()} an
orthonormal basis of the components' shared null space, \code{pen@n_coef} by
\code{pen@n_coef - penalty_rank(pen)}.
\code{penalty_logpdet()} a list of \code{value} (a single number), \code{grad} (a list
keyed by \code{pen@params}) and \code{hess} (a list keyed by the pairs
\code{penalty_hess_theta()} uses), which is the shape the quadratic and
structured branches answer in.
}
\description{
Three of the four pieces a marginal criterion reads. \code{penalty_matrix()}
returns the weighted sum \eqn{S(\lambda) = \sum_k \lambda_k P_k},
\code{penalty_rank()} the rank fixed at construction, and \code{penalty_logpdet()} the
log pseudo-determinant with its first two derivatives. There is no
\code{penalty_null_basis()} method for this branch, and the base class's rejects.
}
\details{
With \eqn{S^{+}} the pseudo-inverse over the stored rank,

\deqn{\frac{\partial}{\partial\lambda_k}\log\mathrm{pdet}\,S
    = \operatorname{tr}(S^{+}P_k), \qquad
  \frac{\partial^{2}}{\partial\lambda_k\partial\lambda_l}
    \log\mathrm{pdet}\,S = -\operatorname{tr}(S^{+}P_kS^{+}P_l),}

both exact and both agreeing with the traces computed apart to 0.

\strong{\code{penalty_logpdet()} answers in a different shape here.} \code{grad} is an
unnamed numeric vector in \code{pen@params} order and \code{hess} is a square matrix,
where \code{\link[=penalty_logpdet.QuadraticPenalty]{penalty_logpdet.QuadraticPenalty()}} and
\code{\link[=penalty_logpdet.StructuredPenalty]{penalty_logpdet.StructuredPenalty()}} return named lists keyed by
hyperparameter and by pair. A consumer written against those will read
\code{NULL} from \code{grad$lambda1} here. Nothing in the toolkit reads it today,
because \code{\link[=is_quadratic]{is_quadratic()}} answers \code{FALSE} for this branch and every consumer
routes on that first.
}
\examples{
P1 <- crossprod(diff(diag(5)))
P2 <- crossprod(diff(diag(5), differences = 2))
pen <- additive_penalty(list(P1, P2))
th <- list(lambda1 = 2, lambda2 = 0.5)

penalty_rank(pen)
lp <- penalty_logpdet(pen, th)
str(lp)

# The gradient is the trace of the pseudo-inverse against each component.
S <- penalty_matrix(pen, th)
e <- eigen(S, symmetric = TRUE)
k <- order(e$values, decreasing = TRUE)[seq_len(penalty_rank(pen))]
Sp <- e$vectors[, k, drop = FALSE] \%*\% (t(e$vectors[, k, drop = FALSE]) /
                                          e$values[k])
max(abs(unlist(lp$grad) - c(sum(Sp * P1), sum(Sp * P2))))

# The null space of the sum is the intersection of the components', so it
# does not move with the hyperparameters.
nb <- penalty_null_basis(pen)
c(width = ncol(nb), annihilated = max(abs(S \%*\% nb)) < 1e-12)

# And the value is the sum of the logarithms of the non-zero eigenvalues.
lp$value - sum(log(e$values[k]))

}
\seealso{
\code{\link[=penalty_matrix]{penalty_matrix()}} and its siblings for the generics,
\code{\link[=additive_sum]{additive_sum()}} for the decomposition these read,
\code{\link[=penalty_matrix.QuadraticPenalty]{penalty_matrix.QuadraticPenalty()}} for the branch whose determinant is
linear in one parameter.
}
\keyword{internal}
