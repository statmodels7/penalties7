% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structured_penalty.R
\name{penalty_kinks.StructuredPenalty}
\alias{penalty_kinks.StructuredPenalty}
\alias{is_proper.StructuredPenalty}
\alias{is_quadratic.StructuredPenalty}
\title{Smoothness and Kind of a Structured Penalty}
\arguments{
\item{pen}{A \code{\link[=StructuredPenalty]{StructuredPenalty()}} object.}

\item{theta}{A named list of the structure's free values. Read by
\code{penalty_kinks()}, whose answer does not depend on it.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
\code{penalty_kinks()} a numeric vector of length zero.
\code{is_quadratic()} the single logical \code{TRUE}.
\code{is_proper()} a single logical, \code{TRUE} when the structure's rank equals
its dimension.
}
\description{
\code{penalty_kinks()} returns \code{numeric(0)}, the value being a quadratic form and
smooth everywhere. \code{is_quadratic()} returns \code{TRUE}, so the four marginal
quantities are available. \code{is_proper()} returns \code{TRUE} when the structure
has full rank.
}
\details{
Properness is the structure's rank against its dimension and nothing else. A
full-rank structure is a proper Gaussian prior; a rank-deficient one, which
is admitted only as a precision, is improper and its constant is the log
pseudo-determinant over the range.
}
\examples{
pen <- structured_penalty(parameters7::ar1(4, role = "precision"))
penalty_kinks(pen, list(log_scale = 0.2, z_rho = 0.5))
is_quadratic(pen)
is_proper(pen)
c(penalty_rank(pen), pen@n_coef)

}
\seealso{
\code{\link[=penalty_kinks]{penalty_kinks()}}, \code{\link[=is_proper]{is_proper()}} and \code{\link[=is_quadratic]{is_quadratic()}} for the
generics, \code{\link[=penalty_matrix.StructuredPenalty]{penalty_matrix.StructuredPenalty()}} for the quantities
\code{is_quadratic()} gates.
}
\keyword{internal}
