% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadratic_penalty.R
\name{penalty_matrix.QuadraticPenalty}
\alias{penalty_matrix.QuadraticPenalty}
\alias{penalty_rank.QuadraticPenalty}
\alias{penalty_null_basis.QuadraticPenalty}
\alias{penalty_logpdet.QuadraticPenalty}
\title{Marginal Quantities of a Quadratic Penalty}
\arguments{
\item{pen}{A \code{\link[=QuadraticPenalty]{QuadraticPenalty()}} object.}

\item{theta}{A list holding \code{lambda}. \code{penalty_matrix()} and
\code{penalty_logpdet()} only.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
\code{penalty_matrix()} a symmetric matrix of side \code{pen@n_coef}, a base
matrix ordinarily and a \code{dgCMatrix} when the penalty was built with
\code{blocks > 1}.
\code{penalty_rank()} a single integer.
\code{penalty_null_basis()} a matrix with \code{pen@n_coef} rows and
\code{pen@n_coef - penalty_rank(pen)} orthonormal columns, sparse under
\code{blocks > 1}.
\code{penalty_logpdet()} a list of \code{value} (a number), \code{grad} (a list of one
number, \code{lambda}) and \code{hess} (a list of one number, \code{lambda_lambda}).
}
\description{
The four pieces a REML or marginal likelihood criterion reads.
\code{penalty_matrix()} returns \eqn{\lambda D'PD}, \code{penalty_rank()} the rank
fixed at construction, \code{penalty_null_basis()} the stored orthonormal basis
of the null space, and \code{penalty_logpdet()} the log pseudo-determinant with
its first two derivatives in \eqn{\lambda}.
}
\details{
Since \eqn{S(\lambda) = \lambda D'PD} and only the range contributes,

\deqn{\log^{+}\lvert S(\lambda) \rvert = r\log\lambda
  + \log\mathrm{pdet}(P), \qquad
  \frac{\partial}{\partial\lambda} = \frac{r}{\lambda}, \qquad
  \frac{\partial^2}{\partial\lambda^2} = -\frac{r}{\lambda^2},}

all three exact and none of them needing a decomposition at call time. The
rank and the null basis do not depend on \eqn{\lambda} and their generics
take no \code{theta}.
}
\examples{
pen <- quadratic_penalty(crossprod(diff(diag(5), differences = 2)))
penalty_rank(pen)

# The null basis spans the straight lines, and the matrix kills it.
N <- penalty_null_basis(pen)
dim(N)
max(abs(penalty_matrix(pen, list(lambda = 3)) \%*\% N))

# The log pseudo-determinant is r log(lambda) plus a constant.
lp <- penalty_logpdet(pen, list(lambda = 3))
c(lp$grad$lambda, lp$hess$lambda_lambda)
c(penalty_rank(pen) / 3, -penalty_rank(pen) / 9)

}
\seealso{
\code{\link[=penalty_matrix]{penalty_matrix()}} and its three siblings for the generics,
\code{\link[=is_quadratic]{is_quadratic()}} for the predicate that gates them,
\code{\link[=penalty_value.QuadraticPenalty]{penalty_value.QuadraticPenalty()}} for the value that carries the same
constant.
}
\keyword{internal}
