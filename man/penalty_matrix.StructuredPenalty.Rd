% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structured_penalty.R
\name{penalty_matrix.StructuredPenalty}
\alias{penalty_matrix.StructuredPenalty}
\alias{penalty_rank.StructuredPenalty}
\alias{penalty_null_basis.StructuredPenalty}
\alias{penalty_logpdet.StructuredPenalty}
\title{Marginal Quantities of a Structured Penalty}
\arguments{
\item{pen}{A \code{\link[=StructuredPenalty]{StructuredPenalty()}} object.}

\item{theta}{A named list of the structure's free values.
\code{penalty_matrix()} and \code{penalty_logpdet()} only.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
\code{penalty_matrix()} the symmetric precision matrix, of side
\code{pen@n_coef}, carrying the structure's own dimnames.
\code{penalty_rank()} a single integer, the structure's rank.
\code{penalty_null_basis()} a matrix with \code{pen@n_coef} rows and one column per
null direction, with no columns for a full-rank structure.
\code{penalty_logpdet()} a list of \code{value} (a number), \code{grad} (a list of one
number per free value) and \code{hess} (a list of one number per unordered
pair, keyed diagonals first).
}
\description{
The four pieces a REML or marginal likelihood criterion reads.
\code{penalty_matrix()} returns the precision \eqn{\Omega(\theta)},
\code{penalty_rank()} the structure's rank, \code{penalty_null_basis()} its null
basis, and \code{penalty_logpdet()} the log pseudo-determinant of the precision
with its first two derivatives in the free values.
}
\details{
Where a plain quadratic penalty has \eqn{\log^{+}\lvert S\rvert} linear in
\eqn{\log\lambda}, here the log-determinant moves with every free value and
its derivatives come from the structure's own log-determinant contract,
negated at every order when the structure describes a covariance.

The rank and the null basis are properties of the structure and do not
depend on \eqn{\theta}: a \code{matrix_parameter} records them at construction,
so a family whose rank is deficient is deficient in the same directions at
every free vector. \code{penalty_null_basis()} reshapes a vector-valued null
basis into a one-column matrix, so its return is always a matrix.
}
\examples{
pen <- structured_penalty(parameters7::ar1(4, role = "precision"))
th <- list(log_scale = 0.2, z_rho = 0.5)

penalty_rank(pen)
dim(penalty_null_basis(pen))

# The matrix is the precision itself, so it is the Hessian of the value.
all.equal(penalty_matrix(pen, th), penalty_hessian(pen, c(0, 0, 0, 0), th))

# And its log pseudo-determinant is the ordinary log-determinant here,
# the structure being full rank.
lp <- penalty_logpdet(pen, th)
lp$value - as.numeric(
  determinant(penalty_matrix(pen, th), logarithm = TRUE)$modulus)
names(lp$hess)

}
\seealso{
\code{\link[=penalty_matrix]{penalty_matrix()}} and its three siblings for the generics,
\code{\link[=struct_omega]{struct_omega()}} for where the derivatives come from,
\code{\link[=penalty_matrix.QuadraticPenalty]{penalty_matrix.QuadraticPenalty()}} for the branch whose determinant is
linear in one parameter.
}
\keyword{internal}
