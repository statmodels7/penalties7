% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prox.R
\name{.prox_param}
\alias{.prox_param}
\title{The Value of a Parent's Parameter, Free or Held}
\usage{
.prox_param(pen, theta, name)
}
\arguments{
\item{pen}{A \code{\link[=DistribPenalty]{DistribPenalty()}} object.}

\item{theta}{The aligned hyperparameter list, as \code{\link[=align_ptheta]{align_ptheta()}} returns it.}

\item{name}{The parameter's name, a single string.}
}
\value{
A single number.
}
\description{
Returns the parent distribution's parameter \code{name}: from the aligned
hyperparameter list when the penalty carries it as a free hyperparameter,
and from the parent's \code{fixed_params} when \code{\link[distributions7:fixed]{distributions7::fixed()}} holds it
at a value.
}
\details{
The closed forms below need the number, not the place it is kept. A penalty
whose scale is held is the same penalty as one whose scale is free, read at
that scale: \code{penalty_value()}, \code{penalty_gradient()} and \code{penalty_hessian()}
all already answer identically for the two. Reading only \code{theta} made
\code{penalty_prox()} the one generic that did not, and it stopped with
\verb{attempt to select less than one element in get1index} rather than saying so,
because \code{which()} of an empty comparison is \code{integer(0)}.
}
\seealso{
\code{\link[=penalty_prox.DistribPenalty]{penalty_prox.DistribPenalty()}}, \code{\link[=penalty_prox_spec]{penalty_prox_spec()}}
}
\keyword{internal}
