% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structured_penalty.R
\name{penalty_gradient.StructuredPenalty}
\alias{penalty_gradient.StructuredPenalty}
\alias{penalty_hessian.StructuredPenalty}
\title{Coefficient Derivatives of a Structured Penalty}
\arguments{
\item{pen}{A \code{\link[=StructuredPenalty]{StructuredPenalty()}} object.}

\item{beta}{A numeric vector of length \code{pen@n_coef}.}

\item{theta}{A named list of the structure's free values.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
\code{penalty_gradient()} an unnamed numeric vector of length
\code{pen@n_coef}; \code{penalty_hessian()} the symmetric precision matrix of that
side, \strong{carrying the structure's own dimnames} (\code{v1}, \code{v2}, ... for the
\pkg{parameters7} primitives). The other branches return matrices with no
dimnames, so a comparison across branches wants \code{unname()}.
}
\description{
\code{penalty_gradient()} returns \eqn{\Omega(\theta)\beta} and
\code{penalty_hessian()} returns \eqn{\Omega(\theta)} itself. The value is a
quadratic form in \eqn{\beta}, so the Hessian does not move with the
coefficients and the gradient is the Hessian applied to them.
}
\details{
Where the structure is a covariance, both read the inverse it implies, so
each call costs one solve. A caller taking several derivatives at one
\eqn{\theta} pays for that inversion once per generic.
}
\examples{
pen <- structured_penalty(parameters7::ar1(4, role = "precision"))
th <- list(log_scale = 0.2, z_rho = 0.5)
b <- c(0.3, -0.1, 0.4, 0.2)

# The gradient is the constant Hessian applied to the coefficients. The
# matrix carries the structure's dimnames and the vector does not.
all.equal(penalty_gradient(pen, b, th),
          unname(drop(penalty_hessian(pen, b, th) \%*\% b)))
dimnames(penalty_hessian(pen, b, th))[[1]]

# And the Hessian is the structure's own matrix.
Om <- parameters7::param_value(parameters7::ar1(4, role = "precision"),
                               c(0.2, 0.5))
max(abs(penalty_hessian(pen, b, th) - unclass(Om)))

}
\seealso{
\code{\link[=penalty_value.StructuredPenalty]{penalty_value.StructuredPenalty()}} for the quantity
differentiated, \code{\link[=penalty_grad_theta.StructuredPenalty]{penalty_grad_theta.StructuredPenalty()}} for the
hyperparameter blocks.
}
\keyword{internal}
