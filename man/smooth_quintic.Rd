% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abs_smoother.R
\name{smooth_quintic}
\alias{smooth_quintic}
\title{The Quintic Smoother of the Absolute Value}
\usage{
smooth_quintic(h = NULL)
}
\arguments{
\item{h}{The transition half-width, a single positive number, or \code{NULL} (the
default) to be resolved at build from the covariate's spacing through
\code{\link[=smoother_width]{smoother_width()}}.}
}
\value{
An \code{\link[=abs_smoother]{abs_smoother()}} named \code{"quintic"}, with \code{width_name} \code{"h"}, no
\code{width_from_spacing}, no \code{tau_correction}, and an \code{exact_radius} equal to
the width.
}
\description{
\eqn{\lvert u\rvert} replaced inside \eqn{[-h, h]} by the even polynomial
matching it to third order at the seam, and \strong{exact} outside. It is the
only smoother here whose bias is zero beyond a finite radius, and the only
one that is not analytic.
}
\section{The closed form}{
With \eqn{t = u/h},

\deqn{s(u) = \frac{h}{16}\,\bigl(5 + 15t^2 - 5t^4 + t^6\bigr)
  \quad \text{for } \lvert t\rvert < 1, \qquad
  s(u) = \lvert u\rvert \quad \text{otherwise},}

so \eqn{s''(u) = 15(1 - t^2)^2/(8h)} vanishes to second order at \eqn{\pm h}
and the function is \eqn{C^3}, with a jump in the fourth derivative at the
seam. At the kink \eqn{s(0) = 5h/16}.
}

\section{Exact outside the transition}{
The smoothing bias is exactly zero outside \eqn{[-h, h]}, which is the
cleanest fixed-width choice: measured at \eqn{h = 0.3} the excess over
\eqn{\lvert u\rvert} is \code{0} at \eqn{u = 1} and at \eqn{u = 3}, where
\code{\link[=smooth_probit]{smooth_probit()}} leaves \code{6.7e-05} and \code{\link[=smooth_hyperbolic]{smooth_hyperbolic()}} \code{4.4e-02}. The
smoother declares an \code{exact_radius}, so a consumer can say where its answer
is the sharp model's.

The branches are why \code{\link[=abs_smoother]{abs_smoother()}} carries the derivatives as functions:
\code{\link[stats:deriv]{stats::deriv()}} does not read a clamp, and written out in the constructor
the branches are ordinary code.

There is no convolution identity here, so \code{tau_correction} is \code{NULL}.
}

\examples{
sm <- smooth_quintic(h = 0.5)

# Outside the transition it is the absolute value, exactly.
smoother_deriv(sm, 1, order = 0)
smoother_deriv(sm, c(0.6, 1, 4), order = 0) - c(0.6, 1, 4)

# At the kink the value is 5h/16.
smoother_deriv(sm, 0, order = 0)
5 * 0.5 / 16

# The second derivative vanishes to second order at the seam, which is
# what makes it C^3 there.
smoother_deriv(sm, c(0.4, 0.49, 0.5, 0.51), order = 2)

}
\seealso{
\code{\link[=abs_smoother]{abs_smoother()}} for the contract, \code{\link[=smooth_probit]{smooth_probit()}} for the
recommended default, \code{\link[=smooth_hyperbolic]{smooth_hyperbolic()}} for the simplest form,
\code{\link[=check_abs_smoother]{check_abs_smoother()}} to verify it.
}
