% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abs_smoother.R
\name{print.abs_smoother}
\alias{print.abs_smoother}
\title{Print a Smoother of the Absolute Value}
\arguments{
\item{x}{An \code{\link[=abs_smoother]{abs_smoother()}} object.}

\item{...}{Unused, accepted for consistency with \code{\link[=print]{print()}}.}
}
\value{
\code{x}, invisibly.
}
\description{
Writes one line naming the smoother and its width, and adds a line for each
of the two optional properties it declares: a scale correction for a random
break-point, and exactness outside the transition.
}
\details{
The width line reads \verb{h resolved at build} where the smoother carries none,
and names the width parameter by its own name, so a hyperbolic smoother
shows \code{c} where a probit shows \code{h}. \verb{per group} is appended when the width
is to be resolved per group.
}
\examples{
smooth_probit()
smooth_probit(h = 0.2, per_group = TRUE)
smooth_hyperbolic()
smooth_quintic(h = 0.4)

}
\keyword{internal}
