% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distrib_penalty.R
\name{penalty_grad_theta.DistribPenalty}
\alias{penalty_grad_theta.DistribPenalty}
\alias{penalty_hess_theta.DistribPenalty}
\alias{penalty_cross.DistribPenalty}
\title{Hyperparameter Derivatives of a Separable Penalty}
\arguments{
\item{pen}{A \code{\link[=DistribPenalty]{DistribPenalty()}} object.}

\item{beta}{A numeric vector of length \code{pen@n_coef}.}

\item{theta}{A named list of the parent's free parameters.}

\item{scale}{Read by the generic, which applies the chain rule onto the
unconstrained scale after dispatch, using the parent's own links. These
methods always return the parameter scale.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
\code{penalty_grad_theta()} a list of one number per hyperparameter,
named by \code{pen@params}.
\code{penalty_hess_theta()} a list of one number per unordered pair, keyed
diagonals first.
\code{penalty_cross()} a list of one numeric vector of length \code{pen@n_coef} per
hyperparameter.
}
\description{
The parent's own score and information in its parameters, negated and summed
over the blocks, plus the mixed block.
\code{penalty_grad_theta()} returns \eqn{-\sum_i \ell^{(\theta_k)}},
\code{penalty_hess_theta()} returns \eqn{-\sum_i \ell^{(\theta_k\theta_l)}}, and
\code{penalty_cross()} returns \eqn{-D'\ell^{(y\theta_k)}}, one coefficient
vector per hyperparameter.
}
\details{
The first two are \code{\link[distributions7:distrib_gradient]{distributions7::distrib_gradient()}} and
\code{\link[distributions7:distrib_hessian]{distributions7::distrib_hessian()}} read at the blocks and summed, the map
not entering: a hyperparameter of the prior does not act through \eqn{D}.
The third is \code{\link[distributions7:distrib_cross_y]{distributions7::distrib_cross_y()}}, which exists for this,
carried back through the map like the coefficient gradient.
}
\section{The reordering}{
\pkg{distributions7} keys its Hessian components lexicographically and this
package keys them diagonals first, so the second derivatives are looked up
under both spellings of each pair, \code{a_b} and \code{b_a}, and returned under this
package's. The key is composed from the pair rather than parsed out of a
name, a parameter name being free to contain an underscore.
}

\examples{
b <- c(1, 0, -1)
pen <- lasso_penalty(n_coef = 3)

# The lasso's value is lambda * L1 - q log(lambda/2), so its derivative in
# lambda is L1 - q/lambda and the second is q/lambda^2.
penalty_grad_theta(pen, b, list(lambda = 0.5))
sum(abs(b)) - 3 / 0.5
penalty_hess_theta(pen, b, list(lambda = 0.5))
3 / 0.5^2

# The mixed block is the sign vector, the value being lambda times the L1
# norm in every coefficient direction.
penalty_cross(pen, b, list(lambda = 0.5))

# An elastic net has two hyperparameters, so three Hessian keys.
names(penalty_hess_theta(elasticnet_penalty(n_coef = 3), b,
                         list(lambda = 1, alpha = 0.4)))

}
\seealso{
\code{\link[=penalty_value.DistribPenalty]{penalty_value.DistribPenalty()}} for the quantity differentiated,
\code{\link[=penalty_grad_theta]{penalty_grad_theta()}} for the generic and its two scales,
\code{\link[distributions7:distrib_cross_y]{distributions7::distrib_cross_y()}} for the mixed block's source.
}
\keyword{internal}
