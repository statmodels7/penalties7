% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scad_mcp.R
\name{penalty_grad_theta.McpPenalty}
\alias{penalty_grad_theta.McpPenalty}
\alias{penalty_hess_theta.McpPenalty}
\alias{penalty_cross.McpPenalty}
\title{Hyperparameter Derivatives of an MCP Penalty}
\arguments{
\item{pen}{An \code{\link[=McpPenalty]{McpPenalty()}} object.}

\item{beta}{A numeric vector of length \code{pen@n_coef}.}

\item{theta}{A list holding \code{lambda} and \code{gamma}.}

\item{scale}{Read by the generic, which applies the chain rule onto the
unconstrained scale after dispatch. These methods always return the
parameter scale.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
\code{penalty_grad_theta()} a list of two numbers, \code{lambda} and \code{gamma}.
\code{penalty_hess_theta()} a list of three numbers, \code{lambda_lambda},
\code{gamma_gamma} and \code{lambda_gamma}.
\code{penalty_cross()} a list of two numeric vectors of length \code{pen@n_coef},
named \code{lambda} and \code{gamma}.
}
\description{
The three blocks in \eqn{(\lambda, \gamma)}, each a closed piecewise form
summed over the coordinates. \code{penalty_grad_theta()} returns the two first
derivatives, \code{penalty_hess_theta()} the three second derivatives, and
\code{penalty_cross()} the mixed block, one coefficient vector per
hyperparameter.
}
\details{
Inside the taper the value is \eqn{\lambda u - u^2/(2\gamma)}, so
\eqn{\partial\rho/\partial\lambda = u} and
\eqn{\partial\rho/\partial\gamma = u^2/(2\gamma^2)}; beyond it the value is
\eqn{\gamma\lambda^2/2} and the two derivatives are \eqn{\gamma\lambda} and
\eqn{\lambda^2/2}, neither depending on the coefficient. The second
derivative in \eqn{\lambda} is therefore zero inside the taper, the value
being linear in \eqn{\lambda} there.

As for SCAD, these derivatives exist and no marginal criterion can use them:
the expansion such a criterion rests on needs a mode at which the penalty is
twice differentiable, and a mode with a coefficient at zero is on the kink.
}
\examples{
pen <- mcp_penalty(n_coef = 3)
th <- list(lambda = 1, gamma = 3)
b <- c(0.5, 2, 5)

penalty_grad_theta(pen, b, th)

# The value is linear in lambda inside the taper, so only the coordinate
# beyond gamma * lambda contributes to the second derivative.
penalty_hess_theta(pen, b, th)$lambda_lambda

}
\seealso{
\code{\link[=penalty_value.McpPenalty]{penalty_value.McpPenalty()}} for the quantity differentiated,
\code{\link[=penalty_grad_theta]{penalty_grad_theta()}} for the generic and its two scales,
\code{\link[=check_penalty]{check_penalty()}}, which verifies all three.
}
\keyword{internal}
