% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadratic_penalty.R
\name{penalty_gradient.QuadraticPenalty}
\alias{penalty_gradient.QuadraticPenalty}
\alias{penalty_hessian.QuadraticPenalty}
\title{Coefficient Derivatives of a Quadratic Penalty}
\arguments{
\item{pen}{A \code{\link[=QuadraticPenalty]{QuadraticPenalty()}} object.}

\item{beta}{A numeric vector of length \code{pen@n_coef}.}

\item{theta}{A list holding \code{lambda}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
\code{penalty_gradient()} a numeric vector of length \code{pen@n_coef};
\code{penalty_hessian()} a symmetric matrix of that side, a base matrix for an
ordinary penalty and a \code{dgCMatrix} when the penalty was built with
\code{blocks > 1}.
}
\description{
\code{penalty_gradient()} returns \eqn{\lambda D'PD\beta} and \code{penalty_hessian()}
returns \eqn{\lambda D'PD}. Both read the cached \eqn{D'PD}, so neither
touches the map at call time, and the Hessian does not depend on \eqn{\beta}
at all, the value being a quadratic form.
}
\details{
Because the Hessian is constant, the gradient is exactly the Hessian applied
to the coefficients, and a Newton step on this penalty alone reaches the
minimum in one iteration from anywhere.
}
\examples{
pen <- quadratic_penalty(crossprod(diff(diag(4))))
b <- c(1, -0.5, 0.2, 2)

# The gradient is the constant Hessian applied to the coefficients.
all.equal(penalty_gradient(pen, b, list(lambda = 3)),
          drop(penalty_hessian(pen, b, list(lambda = 3)) \%*\% b))

# The Hessian does not move with beta.
all.equal(penalty_hessian(pen, b, list(lambda = 3)),
          penalty_hessian(pen, 0 * b, list(lambda = 3)))

}
\seealso{
\code{\link[=penalty_value.QuadraticPenalty]{penalty_value.QuadraticPenalty()}} for the quantity
differentiated, \code{\link[=penalty_grad_theta.QuadraticPenalty]{penalty_grad_theta.QuadraticPenalty()}} for the
hyperparameter derivatives.
}
\keyword{internal}
