% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abs_smoother.R
\name{smoother_deriv}
\alias{smoother_deriv}
\title{Evaluate a Smoother or One of Its Derivatives}
\usage{
smoother_deriv(smoother, u, width = NULL, order = 0L)
}
\arguments{
\item{smoother}{An \code{\link[=abs_smoother]{abs_smoother()}}. Anything else is rejected.}

\item{u}{A numeric vector of points.}

\item{width}{The width, a single positive number or one per point, or \code{NULL}
(the default) for the smoother's own. A smoother built with \code{width = NULL}
and given none here is rejected, with the two ways to supply one named.}

\item{order}{The derivative order, a whole number from 0 to 5. \code{0L} by
default, which is \eqn{s} itself. Anything outside that range is rejected.}
}
\value{
A numeric vector as long as \code{u}.
}
\description{
Returns \eqn{d^k s/du^k} at the given points, for \eqn{k} from 0 to 5. The
width is the smoother's own unless one is supplied, which is how a consumer
that resolved the width at build evaluates without mutating the object.
}
\details{
A vector \code{width} is one value per point, which is how a per-group width
arrives: a break-point term with \code{per_group = TRUE} resolves one width per
subject and passes the whole vector.

The derivatives are the ones the smoother carries, evaluated directly. None
is differenced, so order five is as accurate as order zero.
}
\examples{
# The smooth sign: odd, bounded by one, and passing through zero.
smoother_deriv(smooth_hyperbolic(c = 1), 0:3, order = 1)

# A per-point width, as a per-group smoother supplies.
smoother_deriv(smooth_probit(), c(-1, 0, 1), width = c(0.1, 0.5, 1),
               order = 0)

# An unresolved width is an error rather than a guess.
try(smoother_deriv(smooth_probit(), 1))

}
\seealso{
\code{\link[=abs_smoother]{abs_smoother()}} for the contract, \code{\link[=smoother_width]{smoother_width()}} for
resolving a width, \code{\link[=check_abs_smoother]{check_abs_smoother()}} for verifying the derivatives.
}
